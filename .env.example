# API Settings
API_TITLE=ATP_Re API
API_VERSION=1.0.0
API_PREFIX=/api/v1

# Server Settings
HOST=0.0.0.0
PORT=8000

# Database Settings (SQL Server)
DB_HOST=localhost
DB_PORT=1433
DB_NAME=ATP_DB
DB_USER=atp_user
DB_PASSWORD=your_password_here

# File Upload Settings
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=104857600
ALLOWED_EXTENSIONS=[".dat", ".log", ".txt", ".bin", ".zip"]

# WebSocket Settings
WS_HEARTBEAT_INTERVAL=30

# CORS Settings
CORS_ORIGINS=["http://localhost:8501", "http://localhost:3000"]

# Logging
LOG_LEVEL=INFO
